server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — client-side routing owns every non-file path.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
